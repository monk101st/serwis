extends layout

block content
  h1= title
  if show
    form(method='post')
      p Jaka jest twoja ulubiona pora roku?
      ul 
        each item in data 
          li 
            label=item.title 
            input(type='radio' name='quiz' value=item.id)
      input(type='submit' value='Głosuj')
      p Aby obejrzeć wyniki oddaj głos...
  else 
    h2 Już brałeś udział w ankiecie 
    p Wyniki: 
    ul 
    each item in data 
      li #{item.title} : #{item.vote} (#{Math.round(item.vote/sum*100)}%)
    p Liczba oddanych głosów: #{sum}